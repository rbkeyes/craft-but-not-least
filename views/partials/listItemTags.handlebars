    <!-- checkbox with tag name -->
    <div class="form-check">
        <input type="checkbox" class="form-check-input" id="{{tag.name}}">
        <label class="form-check-label tag" for="{{tag.name}}">{{tag.name}}</label>
    </div>        
{{!-- for future development: allow user to input additional tags and save to db --}}
{{!-- any added tags would be dynamically created and rendered to the form --}}
